QQN needs to have a configurable oracle and line search, and other settings e.g. max line search iterations
Add console logging and diagnostics/defensive code
The visualization for rosenbrock is all messed up - I suspect the bounds checking to define the color scheme is messing up.
